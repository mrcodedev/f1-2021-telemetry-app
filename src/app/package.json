{
  "name": "f1-2021-app-telemetry",
  "private": true,
  "version": "0.0.1",
  "description": "App of telemetry of game F1 2021 of Codemasters. The app listen the WebSocket server, and shows info in real time",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.7.0",
    "@stencil/postcss": "^2.0.0",
    "@stencil/router": "^1.0.1",
    "@types/jest": "^26.0.24",
    "@types/postcss-preset-env": "^6.7.3",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3",
    "postcss-preset-env": "^6.7.0",
    "puppeteer": "^10.0.0",
    "workbox-build": "^4.3.1"
  },
  "license": "MIT"
}
